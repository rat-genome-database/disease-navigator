apply plugin: 'java'
apply plugin: 'war'

repositories {
  mavenCentral()
}

war {
    archiveName = 'navigator.war'
}

dependencies {
	
	compile fileTree(dir: 'lib', include: '*.jar')
}
